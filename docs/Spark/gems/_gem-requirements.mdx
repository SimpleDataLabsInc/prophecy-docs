export default function Requirements(props) {
  return (
    <div>
      <p>
        **Dependencies**
        <td>{props.packagename}</td>
        
        This table describes the dependencies required for this Gem to run with
        Spark (eg Apache Synapse, EMR, Databricks, etc.). Prophecy needs to
        install these dependencies on the Spark cluster, so this table
        specifically mentions compatibility with Databricks UC Shared clusters.
      </p>
      <table>
        <thead>
          <tr>
            <th>Dependency Requirement</th>
            {props.packageversion && (
              <th>
                <a href="https://docs.prophecy.io/package-hub/">Min version</a>
              </th>
            )}
            {props.scalalibs && <th>ScalaLib min version</th>}
            {props.pythonlibs && <th>PythonLib min version</th>}
            <th>14.3 UC Shared</th>
            <th>15.4 UC Shared</th>
            {props.additional_requirements && <th>Additional requirements</th>}
          </tr>
        </thead>
        <tbody>
          <tr>
            <td>{props.packagename}</td>
            {props.packageversion && <td>{props.packageversion}</td>}
            {props.scalalibs && <td>{props.scalalibs}</td>}
            {props.pythonlibs && <td>{props.pythonlibs}</td>}
            <td>{props.packageversion143}</td>
            <td>{props.packageversion154}</td>
            {props.additional_requirements && (
              <td>{props.additional_requirements}</td>
            )}
          </tr>
        </tbody>
      </table>
    </div>
  );
}
