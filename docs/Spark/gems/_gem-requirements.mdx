export default function Requirements(props) {
  return (
    <div>
      <p>Dependencies</p>
      <ul>
        {props.packageversion && (
          <li>
            {props.packagename}: {props.packageversion}+
          </li>
        )}
        {props.scalalib && <li>ProphecyLibsScala: {props.scalalib}+</li>}
        {props.pythonlib && <li>ProphecyLibsPython: {props.pythonlib}+</li>}
      </ul>
      <p>Spark Compatibility</p>
      <ul>
        <li>Databricks UC Shared: {props.packageversion143}</li>
        {props.additional_requirements && (
          <li>Additional Requirements: {props.additional_requirements}</li>
        )}
      </ul>
    </div>
  );
}
